{
  "name": "fragrances-platform",
  "private": true,
  "workspaces": [
    "apps/server",
    "apps/client"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/server & npm run dev -w apps/client",
    "build": "npm run build -w apps/server && npm run build -w apps/client"
  }
}
